[project]
name = "gilbert-plugin-slack"
version = "1.0.0"
description = "Slack Socket Mode integration for Gilbert"
requires-python = ">=3.12"
dependencies = [
    "slack-bolt>=1.18",
]

[tool.uv]
package = false
