name: model-manager
version: "1.0.0"
description: "Local model manager — browse the Hugging Face GGUF catalog (HF-native sort/search + Recommended overlay) and list installed models via Ollama"

provides:
  - model_manager

requires:
  - local_model_runtime

depends_on: []
