[project]
name = "gilbert-plugin-wyoming-whisper"
version = "0.1.0"
description = "Local streaming STT via Wyoming-protocol Whisper for Gilbert"
requires-python = ">=3.12"
dependencies = [
    "wyoming>=1.9.0",
]

[tool.uv]
package = false
