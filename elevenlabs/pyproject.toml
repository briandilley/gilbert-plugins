[project]
name = "gilbert-plugin-elevenlabs"
version = "1.0.0"
description = "ElevenLabs TTS backend for Gilbert"
requires-python = ">=3.12"
# Uses ``httpx`` which is already a Gilbert core dep.
dependencies = []

[tool.uv]
package = false
