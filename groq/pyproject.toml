[project]
name = "gilbert-plugin-groq"
version = "1.0.0"
description = "Groq AI backend for Gilbert"
requires-python = ">=3.12"
# No third-party deps — talks to Groq's OpenAI-compatible REST API
# directly via ``httpx`` (already a core Gilbert dep).
dependencies = []

[tool.uv]
package = false
