[project]
name = "gilbert-plugin-ngrok"
version = "1.0.0"
description = "Ngrok tunnel backend for Gilbert"
requires-python = ">=3.12"
dependencies = [
    "pyngrok>=8.0.0",
]

[tool.uv]
package = false
