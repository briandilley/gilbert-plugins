[project]
name = "gilbert-plugin-anthropic"
version = "1.0.0"
description = "Anthropic Claude AI and Vision backends for Gilbert"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.89.0",
]

[tool.uv]
package = false
