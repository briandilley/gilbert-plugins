[project]
name = "gilbert-plugin-google"
version = "1.0.0"
description = "Google integration suite (auth, directory, Gmail, Drive) for Gilbert"
requires-python = ">=3.12"
dependencies = [
    "google-auth>=2.49.1",
    "google-api-python-client>=2.193.0",
]

[tool.uv]
package = false
