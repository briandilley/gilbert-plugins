name: google
version: "1.0.0"
description: "Google integration suite — OAuth auth, Workspace directory, Gmail, and Google Drive documents"

provides:
  - google_auth
  - google_directory
  - gmail
  - google_drive

requires: []

depends_on: []
