[project]
name = "gilbert-plugin-voice-agent"
version = "0.2.0"
description = "Browser voice-conversation UI (UI-only — backend lives in core VoicePipelineService)."
requires-python = ">=3.12"
# No third-party deps — this plugin is UI-only; all backend
# orchestration lives in core VoicePipelineService.
dependencies = []

[tool.uv]
package = false
